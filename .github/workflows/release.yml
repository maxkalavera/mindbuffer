name: Release
on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  discussions: write

jobs:
#  publish_on_linux:
#    runs-on: ubuntu-latest
#    steps:
#    - uses: actions/checkout@v4
#    - uses: actions/setup-node@v4
#      with:
#        node-version: '20'
#    - name: update apt
#      run: sudo apt update
#    - name: install linux dependencies
#      run: sudo apt-get install build-essential clang libdbus-1-dev libgtk-3-dev \
#        libnotify-dev libasound2-dev libcap-dev \
#        libcups2-dev libxtst-dev \
#        libxss1 libnss3-dev gcc-multilib g++-multilib curl \
#        gperf bison python3-dbusmock openjdk-8-jre
#    - name: install dependencies
#      run: yarn install
#    - name: publish
#      env:
#        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} 
#      run: yarn run publish

  publish_on_mac:
    runs-on: macos-latest

    steps:
    - uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: latest-stable
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: '20.11.x'
    - name: install rosetta
      run: softwareupdate --install-rosetta --agree-to-license
    - name: install dependencies
      run: yarn install
    - name: build current system distributables
      run: yarn run build:darwin
    - name: Release
      uses: softprops/action-gh-release@v2
      if: startsWith(github.ref, 'refs/tags/')
      with:
        files: |
          ${{ github.workspace }}/.package/distribution/MindBuffer-mac-x64.dmg
          ${{ github.workspace }}/.package/distribution/MindBuffer-mac-arm64.dmg

#  publish_on_win:
#    runs-on: windows-latest
#    steps:
#    - uses: actions/checkout@v4
#    - uses: actions/setup-node@v4
#      with:
#        node-version: '20'
#    - name: install dependencies
#      run: yarn install
#    - name: publish
#      env:
#        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#      run: yarn run publish